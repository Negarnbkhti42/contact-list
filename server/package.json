{
  "name": "contact-list-server",
  "version": "1.0.0",
  "description": "server for contact list app",
  "main": "index.js",
  "scripts": {
    "start": "json-server index.json --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Negar Nobakhti",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
